# HugoBricks favicon fix

This patch fixes the Hugo error:

`nil pointer evaluating resource.Resource.Resize`

Copy the `assets` and `data` folders into the root of your existing `hugobricks` project and replace files when asked.

Then run:

```bash
hugo server
```
